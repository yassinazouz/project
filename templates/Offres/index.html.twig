{% extends 'base.html.twig' %}

{% block title %}Nos Offers
{% endblock %}

{% block body %}
	<h1>Liste des Offers</h1>
	<table class="table">
		<tr>
			<th>Titre</th>
			<th>ISBN</th>
			<th>Editeur</th>
			<th>Date Edition</th>
			<th>Prix</th>
			<th>Catégorie</th>
			<th colspan="3">Action</th>
		</tr>
		{% for Offre in Offres %}
			<tr>
				<td>{{Offre.titre}}</td>
				<td>{{Offre.ISBN}}</td>
				<td>{{Offre.editeur}}</td>
				<td>{{Offre.dateEdition|date('d-m-Y') }}</td>
				<td>{{Offre.prix}}</td>
				<td>{{Offre.categorie.libelle}}</td>
				<td>
					<a href="{{ path('app_admin_Offres_show', {'id': Offre.id}) }}" class="btn btn-primary btn-sm">Voir plus...</a>
				</td>
				<td>
					<a href="{{ path('app_admin_Offres_delete', {'id': Offre.id}) }}" class="btn btn-danger btn-sm">Supprimer</a>
				</td>
				<td>
					<a href="{{ path('app_admin_Offres_update', {'id': Offre.id}) }}" class="btn btn-success btn-sm">Editer</a>
				</td>
			</tr>

		{% endfor %}

	</table>

{% endblock %}
